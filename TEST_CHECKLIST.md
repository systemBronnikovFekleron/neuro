# Чек-лист тестирования BronnikovExerciseApp

**Дата:** 2026-02-04
**Версия:** MVP v0.7
**Устройство:** NeiryBand

---

## 🎯 КРИТИЧНЫЕ ФУНКЦИИ (Блокирующие)

### 1. Подключение к устройству
- [ ] **Capsule backend подключается** (inproc://capsule)
- [ ] **Устройство обнаруживается** через Bluetooth
- [ ] **Подключение к NeiryBand успешно**
- [ ] **Батарея показывает уровень** (например, 73%)
- [ ] **Качество сигнала** определяется

**Ожидаемый результат:**
```
✓ Успешное подключение к Capsule backend
✓ Найдено устройств: 1
  1. NeiryBand_XXXX
✓ Подключено к устройству: NeiryBand_XXXX
Батарея устройства: XX%
Качество сигнала: XX%
```

**Что делать при ошибке:**
- Проверить Bluetooth включен
- Убедиться что устройство заряжено
- Перезапустить приложение
- Проверить что libCapsuleClient.dylib в build/Release/

---

### 2. Создание EEG сессии
- [ ] **Сессия создается** без ошибок
- [ ] **Классификаторы инициализируются**:
  - [ ] CNFBMetricProductivity
  - [ ] CNFB (Alpha/Beta/Theta)
  - [ ] CEmotions
  - [ ] CCardio
- [ ] **Режимы устройства переключаются**:
  - [ ] Signal mode (EEG)
  - [ ] PPG mode (пульс)

**Ожидаемый вывод:**
```
[MetricsCollector] >>> CNFB INITIALIZED! <<<
[MetricsCollector] Запуск обучения модели CNFB...
[MetricsCollector] >>> CNFB MODEL TRAINED! <<<
[MetricsCollector] Добавление feedback функций (alpha, beta, theta)...
[MetricsCollector] Инициализация CEmotions...
[MetricsCollector] >>> CEmotions INITIALIZED! <<<
[MetricsCollector] Запуск калибровки CEmotions...
✓ Все системы готовы!
```

**Что проверить:**
- Нет ошибок вида "✗ Не удалось создать..."
- Все классификаторы инициализированы
- Callbacks вызываются

---

### 3. База данных (SQLite)
- [ ] **БД создается** автоматически в `data/bronnikov_sessions.db`
- [ ] **Профиль пользователя создается** при первом запуске
- [ ] **Профиль загружается** при повторном запуске

**Ожидаемый вывод (первый запуск):**
```
[SessionDatabase] ✓ БД открыта: data/bronnikov_sessions.db
📝 Создаем новый профиль пользователя...
[SessionDatabase] ✓ Профиль сохранен: default
```

**Ожидаемый вывод (повторный запуск):**
```
[SessionDatabase] ✓ БД открыта: data/bronnikov_sessions.db
[SessionDatabase] ✓ Профиль загружен: default
✓ Профиль загружен: Пользователь
```

**Проверка после запуска:**
```bash
sqlite3 data/bronnikov_sessions.db "SELECT * FROM users;"
sqlite3 data/bronnikov_sessions.db "SELECT * FROM sessions;"
```

---

## ⚡ ОСНОВНЫЕ ФУНКЦИИ

### 4. Exercise Library (Меню упражнений)
- [ ] **Меню отображается** с 10 упражнениями
- [ ] **Можно выбрать любое** упражнение (1-10)
- [ ] **Некорректный ввод обрабатывается** (не падает)
- [ ] **Выход по 0** работает

**Тест:**
1. Запустить приложение
2. Дождаться меню упражнений
3. Попробовать ввести: "abc", "99", "-1" → должно переспросить
4. Выбрать упражнение 1 → должно начаться

---

### 5. Калибровка NFB (опционально)
- [ ] **Калибровка запускается** по нажатию "y"
- [ ] **Прогресс показывается** (точки каждую секунду)
- [ ] **Callback OnIndividualNFBCalibrated вызывается**
- [ ] **IAF определяется** (например, 10.2 Hz)
- [ ] **IAF сохраняется в профиль**

**Ожидаемый вывод:**
```
Запустить калибровку NFB? (y/n): y
Запуск калибровки (90 секунд)...
Сядьте удобно, закройте глаза и расслабьтесь.
Прогресс калибровки: .......... 10с.......... 20с...
✓ Калибровка завершена!
Ваша индивидуальная альфа-частота (IAF): 10.X Hz
[SessionDatabase] ✓ Профиль сохранен: default
```

**Проверка:**
```bash
sqlite3 data/bronnikov_sessions.db "SELECT iaf, last_calibration_date FROM users WHERE user_id='default';"
```

---

### 6. Baseline метрики
- [ ] **Baseline создается** перед упражнением
- [ ] **Сохраняется в профиль**
- [ ] **Загружается при следующем запуске**

**Ожидаемый вывод:**
```
Создание baseline метрик...
✓ Baseline сохранен в профиль
```

**Проверка:**
```bash
sqlite3 data/bronnikov_sessions.db "SELECT baseline_alpha, baseline_beta FROM users WHERE user_id='default';"
```

---

### 7. Выполнение упражнения "Энергетический шар"
- [ ] **Упражнение стартует** после выбора
- [ ] **Инструкции показываются** по фазам
- [ ] **Метрики обновляются** каждые 10 секунд
- [ ] **Alpha/Beta/Theta показывают значения** (не 0)
- [ ] **Concentration/Relaxation работают**
- [ ] **Heart Rate определяется** (PPG)

**Тест метрик каждые 10 секунд:**
```
=== Текущие метрики ===
Альфа:         0.XXX  ← ДОЛЖНО БЫТЬ > 0
Бета:          0.XXX  ← ДОЛЖНО БЫТЬ > 0
Тета:          0.XXX  ← ДОЛЖНО БЫТЬ > 0
Концентрация:  XX.X   ← ДОЛЖНО БЫТЬ > 0
Релаксация:    XX.X   ← ДОЛЖНО БЫТЬ > 0
Усталость:     XX.X
Фокус:         X.X    ← ПРОВЕРЯЕМ CEMOTIONS
Стресс:        X.X    ← ПРОВЕРЯЕМ CEMOTIONS
Пульс:         XXX уд/мин ← ДОЛЖНО БЫТЬ > 0
```

**КРИТИЧНО ПРОВЕРИТЬ:**
- Альфа/Бета/Тета **НЕ равны 0** (были проблемы раньше)
- Пульс определяется (был 101 BPM)
- Фокус/Стресс **больше 0** или все еще 0? (CEmotions)

---

### 8. CSV логирование
- [ ] **CSV файл создается** с timestamp
- [ ] **Данные записываются** каждое обновление
- [ ] **Формат CSV корректный** (17 колонок)

**Проверка:**
```bash
ls -lh session_*.csv | tail -1
head -5 session_*.csv
tail -5 session_*.csv
```

**Ожидаемый формат:**
```csv
timestamp,alpha_power,beta_power,theta_power,alpha_beta_ratio,concentration,relaxation,fatigue,gravity,focus,chill,stress,anger,self_control,heart_rate,stress_index,iaf
1707088946.123,0.234,0.187,0.289,1.251,45.2,38.7,12.3,0.5,2.1,3.4,1.2,0.8,4.5,102.3,15.2,10.1
```

---

### 9. Сохранение сессии в БД
- [ ] **SessionRecord создается** после завершения
- [ ] **Сохраняется в таблицу sessions**
- [ ] **Все метрики заполнены**

**Ожидаемый вывод:**
```
💾 Сессия сохранена в базу данных
[SessionDatabase] ✓ Сессия сохранена: Энергетический шар (Success: XX%)
```

**Проверка:**
```bash
sqlite3 data/bronnikov_sessions.db "SELECT exercise_name, duration_seconds, success_rate, avg_alpha FROM sessions ORDER BY timestamp DESC LIMIT 1;"
```

---

### 10. Результаты упражнения
- [ ] **Статистика отображается**:
  - [ ] Общее время
  - [ ] Время в целевом состоянии
  - [ ] Success Rate
  - [ ] Изменения от baseline (%, со знаком)

**Ожидаемый вывод:**
```
═══════════════════════════════════════════════════════════════
  Результаты упражнения: Энергетический шар
═══════════════════════════════════════════════════════════════
Общее время:               XXX сек
Время в целевом состоянии: XXX сек
Средняя оценка:            XX%
Максимальная оценка:       XX%
Итоговый успех:            XX%

=== Изменения относительно baseline ===
Альфа:        +XX%  (или -XX%)
Бета:         +XX%
Концентрация: +XX%
Релаксация:   +XX%
```

---

## 🔬 НОВЫЕ ФУНКЦИИ (Тестируем сегодня)

### 11. ⭐ CEmotions метрики (ГЛАВНОЕ!)
**Цель:** Проверить, заработали ли Emotions после исправления

- [ ] **CEmotions инициализируется**
- [ ] **Калибровка CEmotions запускается**
- [ ] **Callback OnCalibrated вызывается**
- [ ] **Метрики обновляются** (не 0):
  - [ ] Focus > 0
  - [ ] Chill > 0
  - [ ] Stress > 0
  - [ ] Anger > 0
  - [ ] Self-Control > 0

**Что смотреть в логах:**
```
[MetricsCollector] Инициализация CEmotions...
[MetricsCollector] >>> CEmotions INITIALIZED! <<<
[MetricsCollector] Запуск калибровки CEmotions...
[MetricsCollector] >>> CEmotions CALIBRATED! <<<
[MetricsCollector] ✓ CEmotions готов к работе!
[MetricsCollector]   Метрики эмоций должны начать поступать
```

**Debug вывод каждые 10 callbacks:**
```
[MetricsCollector] CEmotions callback #10 | Focus: X.X | Stress: X.X | Chill: X.X
```

**ЕСЛИ НЕ РАБОТАЕТ:**
- Проверить: появляются ли callbacks вообще?
- Проверить: значения = 0 или просто не вызываются?
- Возможно NeiryBand не поддерживает CEmotions (требует многоканальный EEG?)

---

### 12. ⭐ Меню выбора упражнений
- [ ] **10 упражнений в списке**
- [ ] **Можно выбрать разные** упражнения подряд
- [ ] **После завершения предлагает** выбрать еще

**Тест:**
1. Выбрать упражнение №1 → выполнить
2. После завершения → выбрать "1" (другое упражнение)
3. Выбрать упражнение №2 → выполнить
4. После завершения → выбрать "0" (выход)

---

### 13. ⭐ Профиль сохраняется
- [ ] **Первый запуск:** профиль создается
- [ ] **Второй запуск:** профиль загружается
- [ ] **IAF сохраняется** после калибровки
- [ ] **Baseline сохраняется** между сессиями

**Тест:**
1. Первый запуск → пройти калибровку → записать baseline → выйти
2. Проверить БД:
   ```bash
   sqlite3 data/bronnikov_sessions.db "SELECT * FROM users;"
   ```
3. Второй запуск → убедиться что IAF и baseline загрузились

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### Проблемы из предыдущих версий:
1. ❌ **CEmotions = 0** → исправлено (теперь тестируем!)
2. ✅ **Alpha/Beta/Theta работают** (было 0.212-0.300)
3. ✅ **Батарея показывает** (было 74% → 73%)
4. ✅ **Пульс работает** (был 101.695 BPM)

### Возможные новые проблемы:
- ⚠️ **БД не создается** → проверить права на data/
- ⚠️ **Профиль не загружается** → проверить SQL запросы
- ⚠️ **Crash при выборе упражнения** → проверить memory leaks

---

## 📝 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Подготовка устройства
```bash
# Убедитесь что NeiryBand:
- Заряжен (>50%)
- Bluetooth включен
- Рядом с компьютером
```

### Шаг 2: Запуск приложения
```bash
cd /Users/avdemkin/code_vibe/Neuro/BronnikovExerciseApp
./build/Release/BronnikovExerciseApp 2>&1 | tee test_run_$(date +%Y%m%d_%H%M%S).log
```

### Шаг 3: Проверка базовой работы
1. Дождаться подключения к устройству
2. Проверить что батарея показывается
3. Дождаться инициализации классификаторов
4. Проверить создание БД и профиля

### Шаг 4: Калибровка (опционально)
1. Ввести "y" при запросе калибровки
2. Закрыть глаза, расслабиться на 90 секунд
3. Проверить что IAF определился
4. Проверить сохранение в БД

### Шаг 5: Выбор упражнения
1. Выбрать упражнение №1 (Энергетический шар)
2. Нажать Enter для начала
3. Следовать инструкциям

### Шаг 6: Мониторинг метрик
**ВНИМАТЕЛЬНО СМОТРЕТЬ:**
- Каждые 10 секунд выводятся метрики
- Проверить что Alpha/Beta/Theta > 0
- **ГЛАВНОЕ: проверить Focus/Stress (CEmotions)**
- Проверить что пульс определяется

### Шаг 7: Завершение и проверка
1. Дождаться завершения упражнения (или Ctrl+C)
2. Проверить результаты
3. Проверить что сессия сохранилась в БД
4. Проверить CSV файл

### Шаг 8: Анализ результатов
```bash
# Проверка БД
sqlite3 data/bronnikov_sessions.db ".tables"
sqlite3 data/bronnikov_sessions.db "SELECT * FROM users;"
sqlite3 data/bronnikov_sessions.db "SELECT exercise_name, success_rate FROM sessions;"

# Проверка CSV
ls -lh session_*.csv
tail -20 session_*.csv

# Проверка логов
grep "CEmotions" test_run_*.log
grep "ERROR\|✗" test_run_*.log
```

---

## ✅ КРИТЕРИИ УСПЕХА

### Минимально работающий продукт (MVP):
- [x] Подключение к устройству
- [x] Получение EEG данных
- [x] Alpha/Beta/Theta метрики
- [x] Пульс (Cardio)
- [x] Выбор упражнений
- [x] CSV экспорт
- [x] БД сохранение
- [x] Профиль пользователя
- [ ] CEmotions метрики (проверяем!)

### Успех = если:
1. ✅ Все классификаторы инициализируются
2. ✅ Метрики Alpha/Beta/Theta/HR работают
3. ✅ БД создается и сохраняет данные
4. ✅ Профиль загружается между запусками
5. ⭐ **CEmotions показывают значения > 0** ← ГЛАВНОЕ

### Частичный успех = если:
- CEmotions callbacks вызываются, но значения = 0
- CEmotions не работает из-за ограничений NeiryBand

### Провал = если:
- Приложение падает
- Классификаторы не инициализируются
- БД не создается

---

## 🚀 ГОТОВЫ К ЗАПУСКУ!

**Команда:**
```bash
cd /Users/avdemkin/code_vibe/Neuro/BronnikovExerciseApp
./build/Release/BronnikovExerciseApp
```

**Или с полным логированием:**
```bash
./build/Release/BronnikovExerciseApp 2>&1 | tee test_$(date +%Y%m%d_%H%M%S).log
```

---

**Удачи! 🎯**
